<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="@dimen/interval_max_80">
    <RelativeLayout
        android:layout_marginBottom="@dimen/interval_default"
        android:layout_alignParentBottom="true"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">
        <ImageView
            android:layout_centerVertical="true"
            android:id="@+id/back"
            android:layout_width="wrap_content"
            android:layout_height="30dp"
            android:layout_marginLeft="@dimen/text_size_large"
            android:src="@drawable/back_gray" />

        <TextView
            android:id="@+id/top_custom_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:text="title"
            android:textColor="@color/black"
            android:textSize="@dimen/text_size_large2" />
    </RelativeLayout>
    <View
        android:layout_alignParentBottom="true"
        android:background="@color/content_line"
        android:layout_width="match_parent"
        android:layout_height="2px"/>
</RelativeLayout>